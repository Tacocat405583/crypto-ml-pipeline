# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual env
.venv/
venv

# Local data & models (generated, not source)
data/
models/
*.db
*.sqlite
*.sqlite3
*.parquet

# Secrets
.env

# Parked Airflow/Kafka stack - kept on disk, untracked. Removed from the remote
# 2026-08-22. The negation that used to whitelist infra/.env is gone on purpose.
infra/

# Notebooks / scratch
.ipynb_checkpoints/
scratch/

# Mini Exchange design doc — belongs to its own repo, kept here until it moves.
# Leading slash matters: a bare 'README.md' matches every README at any depth.
/README.md

# Repo audit — a working snapshot, not a source file
AUDIT.md

# OS
.DS_Store
Thumbs.db

# C++ build output
build/
services/**/build/
